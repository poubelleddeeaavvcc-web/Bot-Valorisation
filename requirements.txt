pandas<3  # 3.0 raises instead of upcasting a str into a still-empty (all-NaN, float64) ledger
# column on .at[] assignment (e.g. exit_date on first exit) -- broke the CI job 2026-09-01
numpy
yfinance
requests
lxml
xlrd
tzdata  # zoneinfo (screener/market_hours.py) needs this on Windows -- no OS-level IANA
# database to fall back on there; harmless/redundant on the Linux CI runner, which already
# ships system tzdata, but keeps local dev/testing consistent
